# Instructions for `.agents/`

The project-wide source of truth is `../AGENTS.md`. Follow it for all work in
this directory as well.

When editing Eve integrations or agent definitions, also read the relevant
guide in `node_modules/eve/docs/`.
